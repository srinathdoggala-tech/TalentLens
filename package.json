{
  "name": "talentlens-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend && cp -r frontend/.next .next",
    "start": "npm run start --prefix frontend"
  }
}
